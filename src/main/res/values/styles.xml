<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <!-- Action Bar -->
        <!--<item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>-->
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="popupMenuStyle">@style/PopupMenu</item>
        <item name="actionBarPopupTheme">@style/PopupMenu</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/MyActionBarTabText</item>
        <item name="android:actionMenuTextColor">@color/action_bar_text_color</item>
        <item name="android:windowBackground">@android:color/white</item>

        <item name="colorPrimary">@color/primary</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <!-- colorAccent is used as the default value for colorControlActivated which is used to tint widgets -->
        <item name="colorAccent">@color/accent</item>
        <!-- colorButtonNormal is used as the default value for background buttons -->
        <item name="colorButtonNormal">@color/accent</item>
        <item name="android:textColor">@color/icons</item>
       <!--<item name="android:buttonStyle">@style/ButtonStyle</item>-->

        <!-- Text styles -->
        <item name="android:textViewStyle">@style/TextStyle</item>
        <item name="android:editTextColor">@color/primaryText</item>
        <item name="android:textColorPrimary">@color/primaryText</item>
        <item name="android:textColorSecondary">@color/secondaryText</item>
        <item name="android:textColorPrimaryInverse">@color/primaryLight</item>
        <item name="android:textColorSecondaryInverse">@color/primaryLight</item>

    </style>




    <!-- Main Activity application theme. -->
    <style name="MainTheme" parent="Theme.AppCompat.NoActionBar">

        <!-- Action Bar -->
        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="popupMenuStyle">@style/PopupMenu</item>
        <item name="actionBarPopupTheme">@style/PopupMenu</item>
        <!-- <item name="android:actionBarStyle">@style/MyActionBar</item>
         <item name="android:actionBarTabTextStyle">@style/MyActionBarTabText</item>
         <item name="android:actionMenuTextColor">@color/action_bar_text_color</item>-->
        <item name="android:windowBackground">@android:color/white</item>

    <item name="colorPrimary">@color/primary</item>
    <!-- colorPrimaryDark is used for the status bar -->
    <item name="colorPrimaryDark">@color/primaryDark</item>
    <!-- colorAccent is used as the default value for colorControlActivated which is used to tint widgets -->
    <item name="colorAccent">@color/accent</item>
    <!-- colorButtonNormal is used as the default value for background buttons -->
        <item name="colorButtonNormal">@color/accent</item>
        <item name="android:textColor">@color/icons</item>


        <!--<item name="android:buttonStyle">@style/ButtonStyle</item>-->

        <!-- Text styles -->
        <item name="android:textViewStyle">@style/TextStyle</item>
        <item name="android:editTextColor">@color/icons</item>
        <item name="android:textColorPrimary">@color/icons</item>
        <item name="android:textColorSecondary">@color/secondaryText</item>
        <item name="android:textColorPrimaryInverse">@color/primaryLight</item>
        <item name="android:textColorSecondaryInverse">@color/primaryLight</item>

    </style>

<style name="ButtonStyle" parent="android:Widget.Button">
    <item name="android:background">@color/accent</item>
    <item name="background">@color/accent</item>
    <item name="colorButtonNormal">@color/accent</item>
    <item name="android:textColor">@color/action_bar_background</item>
</style>

<!-- ActionBar styles -->
    <style name="MyActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/action_bar_background</item>
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="android:textColor">@color/action_bar_text_color</item>
    </style>

    <style name="PopupMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:panelBackground">@color/popupmenu_background</item>
        <item name="panelBackground">@color/popupmenu_background</item>
        <item name="android:popupBackground">@color/popupmenu_background</item>
        <item name="android:colorBackground">@color/popupmenu_background</item>
    </style>


    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/action_bar_text_color</item>
    </style>

    <!-- ActionBar tabs text -->
    <style name="MyActionBarTabText" parent="@style/Widget.AppCompat.ActionBar.TabText">
        <item name="android:textColor">@color/action_bar_text_color</item>
        <!-- The textColor property is backward compatible with the Support Library -->
    </style>

    <style name="itemMessageStyle">
        <item name="android:background">@drawable/style_message_background</item>
        <item name="android:padding">@dimen/padding_high</item>
    </style>

    <style name="TextStyle" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="EditText" parent="@android:style/Widget.EditText">
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/primaryLight</item>
        <item name="android:textColorLink">@color/primaryLight</item>
        <item name="android:textColorHighlight">@color/accent</item>
        <item name="android:typeface">normal</item>

        <item name="colorControlNormal">@color/primaryLight</item>
        <item name="colorControlActivated">@color/accent</item>
        <item name="colorControlHighlight">@color/accent</item>

        <item name="android:color">@color/accent</item>
    </style>

</resources>
